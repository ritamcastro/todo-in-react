{
  "name": "drive-the-session",
  "version": "0.0.1",
  "description": "Driving the session",
  "main": "index.js",
  "scripts": {
    "lint": "npx @biomejs/biome check --write ./tests/",
    "pretest": "npx playwright install --with-deps",
    "test": "npx playwright test",
    "test-ui": "playwright test --ui"
  },
  "keywords": [],
  "author": "ritamcastro@gmail.com",
  "license": "ISC",
  "devDependencies": {
    "@biomejs/biome": "^2.2.3",
    "@playwright/test": "^1.55.0",
    "@types/node": "^24.3.1"
  }
}
